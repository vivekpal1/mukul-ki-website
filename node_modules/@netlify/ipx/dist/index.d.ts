import { IPXOptions } from 'ipx';
import { Handler } from '@netlify/functions';
export declare function createIPXHandler({ cacheDir, basePath, propsEncoding, ...opts }?: Partial<IPXOptions> & {
    cacheDir?: string;
    basePath?: string;
    propsEncoding?: 'base64';
}): Handler;
